# Este arquivo está intencionalmente vazio.
# Adicione sua chave SSH privada (`id_rsa`) e pública (`id_rsa.pub`) aqui.
# Lembre-se de definir as permissões corretas para sua chave privada:
# chmod 600 id_rsa
